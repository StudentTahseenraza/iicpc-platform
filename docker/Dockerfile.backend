FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY backend/package*.json ./

# Install dependencies
RUN npm ci || npm install

# Copy source code
COPY backend/src ./src

# Create .env file (will be overridden by docker-compose)
RUN touch .env

EXPOSE 3000

CMD ["npm", "run", "dev"]