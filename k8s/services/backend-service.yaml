apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: iicpc-platform
spec:
  selector:
    app: backend
  ports:
  - port: 3000
    targetPort: 3000
    name: http
  - port: 3001
    targetPort: 3000
    name: metrics
  type: LoadBalancer